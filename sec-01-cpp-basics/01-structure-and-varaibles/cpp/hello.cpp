#include <iostream> // we need this include to use std::cout

int main()      // Every program must have main function
{   // openning the curly brace for function "main"
    
    // Indentation is not needed in C++. 
    // Indentation is for reading purpose.
    std::cout << "Hello world!";

    // My thinking: return 0 can be thought as "0 error", 
    // although the number "0" not about the number of errors.
    return 0;       
}   // closing the curly brace for function "main"