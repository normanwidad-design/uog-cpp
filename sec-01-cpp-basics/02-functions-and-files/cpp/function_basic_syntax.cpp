return_type function_name() // This is the function header 
                            // (tells the compiler about the existence
                            //  of the function)
{
    // This is the function body 
    // (tells the compiler what the function does)
}
// This file is not compilable as it has no main() function